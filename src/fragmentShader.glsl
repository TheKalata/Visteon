R"(
        #version 300 es

        precision mediump float;
        // in vec3 vColor;
        out mediump vec4 fragColor;
        void main()
        {
            fragColor = vec4(1.0, 0.0, 1.0, 1.0);
        }
    )"